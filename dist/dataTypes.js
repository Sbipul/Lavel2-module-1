"use strict";
const names = ["bipul", "mithun", "rabbi"];
const couple = ["Bipul", 2301028001];
const user = {
    name: "Bipul",
    roll: 2301028001,
    gf: true,
    wife: false,
};
const arr = [4, 5, 8];
const newArray = arr.map((item) => item * item);
